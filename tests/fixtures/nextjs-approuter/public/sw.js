// Service worker generated by a PWA plugin (e.g. next-pwa). public/ is the
// standard create-next-app static-assets directory; a PWA setup writes its
// worker file here alongside the default svg icons.
self.addEventListener("install", () => self.skipWaiting());
