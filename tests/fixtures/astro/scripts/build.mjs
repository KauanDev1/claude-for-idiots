// A common Astro addition: a Node build-time script (e.g. RSS feed
// generation, content migration) run via `node scripts/build.mjs` from an
// npm script, outside Astro's own src/ pipeline.
console.log("build step");
